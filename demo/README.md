# Demo

Minimal Next.js + Material UI demo for Aleo Dev Doctor.

## Purpose

The demo explains project value using sample diagnostics output. It does not run local CLI checks in the browser.

## Run

```bash
npm --prefix demo install
npm --prefix demo run dev
```

Then open `http://localhost:3000`.
