# Sample Program Fixture

This fixture is used by Aleo Dev Doctor to validate the Aleo development lifecycle:

- `leo build`
- `leo run`
- mockable ZK execution readiness
- mockable proof readiness
- mockable verification readiness

Expected commands:

```bash
leo build
leo run main 5u32
```

The CLI uses this folder as a lightweight validation target rather than a full proving environment.
